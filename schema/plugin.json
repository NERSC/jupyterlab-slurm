{
  "title": "Slurm Dashboard",
  "description": "Slurm Dashboard settings",
  "properties": {
    "userOnly": {
      "type": "boolean",
      "title": "My jobs only",
      "description": "Show jobs only from this user",
      "default": false
    },
    "notifyOnStateChange": {
      "type": "boolean",
      "title": "Notify on job state change",
      "description": "Show a notification when one of your own tracked jobs' state changes (e.g. RUNNING to COMPLETED)",
      "default": false
    },
    "autoReload": {
      "type": "boolean",
      "title": "Auto-refresh",
      "description": "Automatically poll squeue periodically for updates",
      "default": true
    },
    "autoReloadRate": {
      "type": "number",
      "title": "Auto-refresh rate",
      "description": "Time to wait (in seconds) in between auto refreshes of squeue",
      "default": 60
    },
    "itemsPerPageAuto": {
      "type": "boolean",
      "title": "Auto page size",
      "description": "Automatically size the page to fit the available vertical space",
      "default": true
    },
    "itemsPerPage": {
      "type": "number",
      "title": "itemsPerPage",
      "description": "Number of rows to display per page of squeue results",
      "default": 10
    },
    "itemsPerPageOptions": {
      "type": "array",
      "title": "itemsPerPageOptions",
      "description": "Selections for number of rows to display per page of squeue results",
      "items": { "type": "number" },
      "default": [10, 15, 20, 25, 30, 40, 50]
    },
    "columnState": {
      "type": "array",
      "title": "Column state",
      "description": "Persisted ag-grid column order, visibility, width and pinning for the queue table. Managed automatically as columns are reordered/resized/hidden; not intended for manual editing.",
      "items": { "type": "object" },
      "default": []
    }
  },
  "additionalProperties": false,
  "type": "object"
}
